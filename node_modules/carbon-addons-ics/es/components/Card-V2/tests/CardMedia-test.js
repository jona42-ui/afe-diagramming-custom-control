"use strict";

var _react = _interopRequireDefault(require("react"));

var _CardMedia = _interopRequireDefault(require("../CardMedia"));

var _enzyme = require("enzyme");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('CardMedia', function () {
  describe('Renders as expected', function () {
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_CardMedia.default, {
      className: "extra-class",
      type: "img",
      src: "some link",
      altText: "some alt text"
    }));
    it('has the expected classes', function () {
      expect(wrapper.hasClass('bx--card__media')).toEqual(true);
    });
    it('renders extra classes passed in via className', function () {
      expect(wrapper.hasClass('extra-class')).toEqual(true);
    });
    it('renders children as expected', function () {
      expect(wrapper.find('.bx--card__media-img').length).toBe(1);
    });
  });
  describe('Check that functions passed in as props are called', function () {
    var onClick = jest.fn();
    var wrapper = (0, _enzyme.mount)(_react.default.createElement(_CardMedia.default, {
      onClick: onClick
    }));
    it('should call onClick', function () {
      wrapper.simulate('click');
      expect(onClick).toBeCalled();
    });
  });
});