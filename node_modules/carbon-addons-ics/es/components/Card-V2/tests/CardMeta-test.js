"use strict";

var _react = _interopRequireDefault(require("react"));

var _CardMeta = _interopRequireDefault(require("../CardMeta"));

var _enzyme = require("enzyme");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('CardMeta', function () {
  describe('Renders as expected', function () {
    var wrapper = (0, _enzyme.shallow)(_react.default.createElement(_CardMeta.default, {
      className: "extra-class",
      metaPrimary: "primary text",
      metaSecondary: "secondary text",
      metaPadding: true
    }));
    it('has the expected classes', function () {
      expect(wrapper.hasClass('bx--card__meta')).toEqual(true);
    });
    it('renders extra classes passed in via className', function () {
      expect(wrapper.hasClass('extra-class')).toEqual(true);
    });
    it('renders pading passed as expected', function () {
      expect(wrapper.hasClass('bx--card__meta-padding')).toEqual(true);
    });
    it('renders primary text as expected', function () {
      expect(wrapper.find('.bx--card__meta-primary').length).toBe(1);
    });
    it('renders secondary as expected', function () {
      expect(wrapper.find('.bx--card__meta-secondary').length).toBe(1);
    });
  });
});